# railway.toml — Laravel + Docker setup
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
healthcheckPath = "/"
healthcheckTimeout = 100
numReplicas = 1

[envs]
APP_ENV = "production"
APP_DEBUG = "false"
